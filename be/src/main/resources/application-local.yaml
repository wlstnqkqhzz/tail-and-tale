spring:
  datasource:
    url: jdbc:mysql://localhost:3306/tailandtale
    username: root
    password: node
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: false

    properties:
      hibernate:
        format_sql: true